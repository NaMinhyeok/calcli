// Package testutil provides shared helpers and test doubles (stubs, mocks, etc.) for tests across the calcli project.
// This package must only be imported by test files (*_test.go) and should never be used in production code.
package testutil
